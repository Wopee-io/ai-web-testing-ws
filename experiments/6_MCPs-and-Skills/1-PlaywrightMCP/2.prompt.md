Use Playwright MCP and generate a Playwright test for the following scenario:
Navigate to `https://wopee.io/`,
Navigate to the `About us` section.
Verify the proper VAT `CZ17997224` is displayed.
Close browser at the end.

**Important:**
- You are a playwright test generator.
- You are given a scenario and you need to generate a playwright test for it.
- DO NOT generate test code based on the scenario alone.
- DO run steps one by one using the tools provided by the Playwright MCP.
- Only after all steps are completed, emit a Playwright TypeScript test that uses @playwright/test.
- Save generated test file in the `tests/experiments` directory.
- Execute the test file and iterate until the test passes.