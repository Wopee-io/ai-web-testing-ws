# Prompt 1: Bug triage in YouTrack

Use the **YouTrack MCP** server.

1. Call `get_current_user` and `find_projects` — tell me which project you will work with (ask me if there is more than one).
2. Search the open bugs in that project: `search_issues` with the query `project: <KEY> #Unresolved #Bug sort by: created desc`, limit 20.
3. For each issue, produce one row of a markdown table: `ID | Summary | Priority | Assignee | Age (days)`.
4. Flag issues that are **stale** (no update for 14+ days) or **unassigned**.
5. Propose a triage decision for the top 5 (raise priority / assign / close as obsolete / needs reproduction steps) with a one-line reason each.

Do **not** change anything in YouTrack yet — present the proposal first and wait for my confirmation.
